//

const Options = ({ children }) => {
  //
  return (
    <div className="Options">
      <h1>Options</h1>

      {/* children = Notifications Component */}
      {children}
    </div>
  );
};

export default Options;
